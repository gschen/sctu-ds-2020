class Solution:
    def mergeKLists(self,list:List[ListNode]) ->ListNode
       L=[]
       for i in lists:
           while i:
               L.append(i.val)
               i=i.next
        l.sort()
        L_node=tem=ListNode(0)
        for i in L:
            tem.next=ListNode(j)
            tem=tem.next
        return L_node.next