class Person():
    def __init__(self,name,blood,crit,avoid):
        self.name=name
        self.blood=blood
        self.crit=crit
        self.avoid=avoid
#攻击
def attack(self,object):
    #对方是否闪避本次攻及
    if random.random >=object.avoid:
        if random.random >=object.avoid:
            object.blood-=self.power*2
        else:
            object..blood-=self.power
class Game():
    def __init__(self,one,two):
        self.one=one
        self.two=two
    def solo(self):
        while True:
            one.att


                           